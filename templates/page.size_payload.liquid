<div class="page-width">
  <div class="grid page-wrapper page-wrapper--size-payload">
    <h1>{{ page.title }}</h1>
    <div class="normal-page-content text-center">
      {% if page.content.size > 0 %}
        {{ page.content }}
      {% endif %}
    </div>
  </div>
</div>
