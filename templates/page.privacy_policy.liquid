<div class="page-width">
  <div class="grid page-wrapper page-wrapper--privacy-policy">
    {% section 'page-sidebar' %}
    
    <div class="normal-page-content">
      <h1>{{ page.title }}</h1>

      <div class="page-privacy-policy-inner">
        {% if page.content.size > 0 %}
          {{ page.content }}
        {% endif %}
      </div>
    </div>
  </div>
</div>
