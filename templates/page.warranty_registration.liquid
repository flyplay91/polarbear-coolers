<div class="page-width">
  <div class="grid page-wrapper page-wrapper--warranty">
    {% section 'page-sidebar' %}
    
    <div class="normal-page-content">
      <h1>{{ page.title }}</h1>

      <div class="page-warranty-inner">
        {% if page.content.size > 0 %}
          {{ page.content }}
        {% endif %}
      </div>
    </div>
  </div>
</div>
